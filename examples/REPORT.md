---
title: Living examples — run report
doc_type: [REPORT]
generated_from: dist/vsl-manifest.json
tier: live
generated_at: 2026-06-25 05:12
---

# Living examples — run report

GENERATED by `tools/run-examples.py` (`make examples-run` / the nightly live cadence) — DO NOT edit by hand. This is the *executed* half of the trust thesis: every example program above (see `index.md`) is shown here passing on real engines, through the driver stack only. The bare tier (`m-test-engine` + `m-test-iris`) gates every PR; the live tier (`vehu` + `foia`) runs on a nightly cadence, fail-soft, with a pre/post **residue check** proving the shared live VistA came back byte-identical.

- **Tier:** `live`  •  **Engines:** `ydb-live`, `iris-live`
- **Generated:** 2026-06-25 05:12
- **Coverage (from the manifest):** 125 public labels across 17 module program(s); 8/9 `@raises` demonstrated. (See `index.md` / `make examples-coverage` for the authoritative executable-vs-illustrative coverage split.)

## Residue check (live tier — §8)

| Engine | Result |
|---|---|
| `ydb-live` | ✅ clean — no owned-global residue (engine byte-identical) |
| `iris-live` | ✅ clean — no owned-global residue (engine byte-identical) |

> Fingerprint = node count + value-bytes under the library's own global roots (`^VSLTAP`, from the namespace registry), counting only **named-key** subtrees via portable `$QUERY`. Integer-keyed subtrees are excluded — that is the M convention for job-scoped scratch (`^STDLIB($JOB,…)`, which the assertion harness writes every suite) and transient ring-buffer sequences (`^VSLTAP(seq)`, rolled back by the tap examples); neither is persistent residue. A new or un-restored named node reds the report. Background VistA churn outside the library's namespace is deliberately not tracked (a shared live engine is never byte-identical moment-to-moment); examples are read-only / self-restoring by construction (E1–E3b) and this is the backstop.

## Pass / fail by module × engine

| Module | Scope | Safety | `ydb-live` | `iris-live` |
|---|---|---|---|---|
| `VSLCFG` | live | transactional | ✅ 3/3 | ✅ 3/3 |
| `VSLFS` | live | transactional | ✅ 6/6 | ✅ 6/6 |
| `VSLHL7TAP` | bare | read-only | — skip | — skip |
| `VSLIO` | live | read-only | ✅ 5/5 | ✅ 5/5 |
| `VSLLOG` | live | transactional | ✅ 5/5 | ✅ 5/5 |
| `VSLRPCTAP` | bare | read-only | — skip | — skip |
| `VSLRPCWRAP` | bare-ydb | transactional | — skip | — skip |
| `VSLS3` | bare | read-only | — skip | — skip |
| `VSLSEC` | dual | read-only | ✅ 7/7 | ✅ 7/7 |
| `VSLTAP` | bare | read-only | — skip | — skip |
| `VSLTAPFC` | bare | read-only | — skip | — skip |
| `VSLTAPHL` | bare | read-only | — skip | — skip |
| `VSLTASK` | live | transactional | ✅ 6/6 | ✅ 6/6 |

**Verdict:** ✅ all green.


# v-stdlib — per-repo memory index

One line per memory file. Content lives in the files, not here.

- [tap-quarantined](tap-quarantined.md) — **⚠ READ FIRST (2026-06-27): the entire prior RPC/HL7→S3 traffic-tap subsystem was QUARANTINED** out of the active VSL library into `quarantine/` (routines `VSLTAP/VSLRPCTAP/VSLRPCWRAP/VSLS3/VSLHL7TAP/VSLTAPFC/VSLTAPHL` + tests/examples/module-docs/guides + s3-testbed), pending its **greenfield replacement `v-rpc-tap`** (fresh `VSLRT*` against live `CALLP^XWBPRS`; the prior code targeted the retired `CALLP^XWBBRK` `{XWB}` seam). Active routine set now **6** (`VSLCFG/VSLFS/VSLIO/VSLLOG/VSLSEC/VSLTASK`); `vsl.build.json` patch 4→5; all gates regenerated + green. **All `fu*`/`phase*`/tap memory entries below describe RETIRED code — journal only, not current design.** See `docs` repo `v-rpc-tap-scalable` + [[never-use-bespoke-installer]].
- [live-capture-fault-stdcrypto](live-capture-fault-stdcrypto.md) — **live-capture fault RESOLVED 2026-06-25**: tap self-disabled (`disabled="fault"`) on vehu because `write1rec^VSLTAP` called `$$sha256^STDCRYPTO` unconditionally but **STDCRYPTO isn't installed on vehu** (ZLINKFILE; VSL*1.0*3 shipped only VSL*). Fixed by **removing crypto from the capture path entirely** (`set hash=""`, no `hc` node) — the stored hash was dead. TDD red→green, 137/0. Superseded by full crypto removal — see [[egress-hash-removed]]. Still owed: live CPRS smoke (deploy fixed VSLTAP via `v pkg install --auto-snapshot` + re-splice + rpc-tail).
- [egress-hash-removed](egress-hash-removed.md) — **tap is now fully STDCRYPTO-free + de-embellished (2026-06-25, owner directive: raw capture, only required features)**: `payload_sha256` removed from the `VSLS3` envelope; `$$verify^VSLTAPFC` deleted; `$$matches`/`$$reconcile` prove fidelity by BYTE-EQUALITY against the source. **`VSLTAPRUN` (the whole periodic fidelity-run task incl. `fidelityNow`) DELETED as over-engineered** — `VSL TAP FIDELITY CADENCE` param dropped, test/doc/example removed. The byte-equality round-trip proof (`$$reconcile`, VSLS3E2ETST) is retained. Tamper-detection tests deleted. See also [[bespoke-install-routines-removed]].
- [bespoke-install-routines-removed](bespoke-install-routines-removed.md) — **VSLTAPBO (bespoke back-out) + VSLBLD (bespoke KIDS build self-description) + VSLENV (KIDS env-check) DELETED 2026-06-25 (owner: "strictly and exclusively the v-pkg methods")**: all called only by tests / the build hook. Reversal is now `v-pkg uninstall` (routines + #9.7/#9.6 + #8989.51 defs); the tap's only un-removed footprint is transient runtime globals `^XTMP("VSLTAP")`/`^VSLTAP` (auto-purge / operator-kill). The KIDS build now has no `envCheck`. KIDS routine list now **13**; DIBRG/quick-start/tap-architecture guides rewritten to the v-pkg-only contract. **Host-side `v pkg wrap-rpc` + `internal/wrapsplice` ALSO deleted in v-pkg 2026-06-25** — bespoke installers forbidden on BOTH sides; install/back-out is strictly `v pkg install`/`uninstall` of the KIDS build. Org directive: `never-use-bespoke-installer`.

- [living-examples-e4](living-examples-e4.md) — **Living Examples E4 (v-stdlib slice) DONE 2026-06-24**: `tools/run-examples.py` (byte-identical sibling of m-stdlib's) EXECUTES the `VSL*EX.m` programs through the driver stack by per-module **`@exrun`** scope. **Engine split (encoded, kickoff-matched):** tap/S3 family = `@exrun bare` (writes its own `^VSLTAP` → not live-safe on shared VistA); **VSLRPCWRAP = `bare-ydb`** (3 transactional tap-capture examples diverge in the IRIS EX-harness — `$$hdr^VSLTAP` finds no record on IRIS; VSLRPCWRAPTST 33/33 proves IRIS); VistA-binding (VSLCFG/BLD/ENV/FS/IO/LOG/TASK) = `@exrun live`; **VSLSEC = dual** (kickoff said live-only but read-only auth passes bare both). All arms green (bare 10/9, live 8/8), residue clean. **The residue check EARNED ITS KEEP:** caught the tap family leaving `^VSLTAP("cfg")` on live → resolved by making them bare-tier (FOLLOW-UP: self-restore cfg if ever live). Builds on [[living-examples-e3]].
- [living-examples-e3](living-examples-e3.md) — **Living Examples E3 (v-stdlib backfill) DONE 2026-06-24**: VSL* coverage **0%→100% (125/125: 113 executable, 12 illustrative)**, all engine-verified — 348 bare (m-test-engine) + 46 live (vehu) assertions, 15 TST suites still green (309/0). 17-agent workflow LIFTED side-effect-safe assertions from `VSL*TST` suites. Engine split: tap/S3/rpc/hl7 = bare; CFG/BLD/FS/IO/LOG/TASK/ENV/SEC = live vehu (read-only / set-restore / capture-seam). Generator gained Pattern-B `do:postconditional`. **GOTCHAS:** single-line `for`+trailing-assert runs per-iteration; 8 private helpers needed `@internal`; VSLSEC.user `$$GET1^DIQ` faults on vehu→illustrative; raises substring must be FULL code; aligned `make lint` to house gate (`m-lint-gate.sh`, style advisory); `src` edit drifts `dist/kids/VSL.kids`. **E3b DONE**: raises 9/9 accounted (VSLTASK.schedule `@raisesnodemo`; the new tag is in m-stdlib's [[living-examples-e3]]/e3b). Builds on [[living-examples-e2]].
- [living-examples-e2](living-examples-e2.md) — **Living Examples E2 (v-stdlib) DONE 2026-06-24**: `mdoc_tags.py` (byte-identical sibling) gains `@fixture`+`@illustrative`; gen-manifest E2 extraction delta on VSL naming (NOT copied verbatim); conditional-emit ⇒ manifest byte-unchanged. Part 2: `gen-examples.py --coverage` advisory + `make examples-coverage` + CI. Builds on [[living-examples-e1]].

- [living-examples-e1](living-examples-e1.md) — **Living Examples E1 (v-stdlib) DONE 2026-06-24**: `gen-examples.py` byte-identical sibling → `examples/` + `index.md` + `data/` home; `make examples`/`examples-check` in gates+ci. **v-stdlib starts at 0/117 executable coverage** (no Pattern-A @examples in VSL source) — the index surfaces the whole backfill job, which is mostly live-VistA + side-effect-safe (E2-E3, the long pole). No DOCTST coexistence (v-stdlib never had doctests). Proposal `proposals/living-executable-examples.md`.

- [stdlib-docs-phase6](stdlib-docs-phase6.md) — **Stdlib-docs Phase 6 (Polish) v-stdlib slice DONE 2026-06-24** (Phase 6 COMPLETE both repos): `docs/guides/quick-start.md` + **`tap-architecture.md`** (the "one missing narrative" — RPC+HL7→S3 tap data-flow + safety model, distinct from the DIBRG ops runbook) + `examples/vslcfg-demo.m`. Fixed the STALE README ("empty scaffold" → 17 modules + the tap). docs-validate 0/0. Stdlib-docs Phases 1–6 all delivered; residual = the deferred Go `m doc` CLI.

- [grammar-spec-from-code-gate](grammar-spec-from-code-gate.md) — **Grammar spec-from-code gate, v-stdlib slice DONE 2026-06-24** (v-stdlib `main`): `tools/mdoc_tags.py` (the 11-tag registry) is a **byte-identical sibling** of m-stdlib's; v-stdlib's `gen-manifest.py` now derives `KNOWN_TAGS` from it (was a hardcoded set). **Behaviour-preserving** — manifest+errors byte-unchanged, golden + manifest-check green. The grammar doc + `gen-grammar.py` + `grammar-check` live in m-stdlib only (v-stdlib keeps its grammar pointer); v-stdlib carries the registry just to feed its generator. Full design: m-stdlib `docs/memory/grammar-spec-from-code-gate.md` + `docs/plans/grammar-spec-from-code-gate-plan.md`. Builds on [[docs-governance-regime-b]].

- [docs-governance-regime-b](docs-governance-regime-b.md) — **Two-regime docs governance DONE 2026-06-23** (v-stdlib `main`; ADR `docs/background/docs-governance-two-regimes-adr.md`). The doc-framework prose validator was mis-governing the machine-generated `docs/modules/` pages — **and had been RED since Phase-1 `adfb4d7`** (my verbatim grammar copy carried m-stdlib-relative links that don't resolve here). Fix: M1 excludes `modules/` from doc-framework; **M2** = own `reference-frontmatter.schema.json` + `check-frontmatter.py` gate (byte-identical sibling of m-stdlib's; 17/17); **M3** = `docs/guides/m-doc-grammar.md` is now a thin pointer to m-stdlib's canonical (GitHub URL) → **docs-validate clean 0 errors** (regression fixed; R-GRAMMAR); **M4** = `gen-skill.py` adds a `## Companion library` cross-ref to m-stdlib (consumer-assembled, no merged docs-repo artifact; R-2SKILLS). GOTCHA the `make`-gates missed docs-validate (separate workflow) — always run the doc-framework validator on docs changes. Found scoping [[stdlib-docs-phase4]] Phase 5.
- [stdlib-docs-phase4](stdlib-docs-phase4.md) — **Stdlib-docs Phase 4 / AC4 DONE 2026-06-23** (v-stdlib `main` + m-stdlib `master`): module-page API bodies are now GENERATED from the manifest. `tools/gen-bodies.py` (byte-identical sibling both repos) maintains a `## API reference` block (signature table + per-label params/returns/raises/@example) between `<!-- BEGIN/END GENERATED API REFERENCE -->` markers — **additive & lossless** (owns only between-markers; m-stdlib's 40 prose pages = 0 deletions / 6302 insertions; hand `## Public API` preserved). v-stdlib's 17 **stubs became real reference pages** (stripped the "lands in Phase 4" placeholder; updated `stub_body` for future). `make docs-bodies` regenerates, `docs-bodies-check` drift-gates (red on a `.m` signature edit not propagated = AC4). Has `--self-test` (insert/idempotent/replace/prose-preserve). **D3 RESOLVED = NO:** don't force `doc-framework`'s prose schema (`additionalProperties:false` + no "reference" type would strip the load-bearing module/labels/errors FM). Wired into `gates` + `ci.yml`. **NEXT: Phase 5** unified cross-library catalog. Builds on [[stdlib-docs-phase3]].
- [stdlib-docs-phase2](stdlib-docs-phase2.md) — **Stdlib-docs Phase 2 / AC2 DONE 2026-06-23** (cross-repo: v-stdlib `main` + m-stdlib `master` + `.github` `main`): the `make docs-check` completeness gate — red when any `src/*.m` module lacks a manifest entry OR a `docs/modules/<module>.md` page. ONE byte-identical `tools/check-docs.py` in both stdlibs (auto-discovers the manifest; per-repo difference is the `tools/docs-check-allow.txt` allow-list). m-stdlib's 5 page-less modules (STDHTTPD/STDHTTPMSG/STDJWT/STDS3/STDSIGV4) allow-listed → ships GREEN (R-GATEBLOCK), red only on unlisted drift; v-stdlib green 17/17 (no allow file). P2.4 = a guarded auto-step baked into `m-ci.yml` (every M caller inherits it, like the engine-access gate; NOT in `engine-free-targets` → no double-run). GOTCHA: `.github` had a stale `engine-access-gate` branch (downgrades the engine image) — landed on `main`. Has a `--self-test` (5 cases). **NEXT: Phase 3** author the 5 m-stdlib pages + REMOVE from the allow-list. Builds on [[stdlib-docs-phase1]].
- [stdlib-docs-phase1](stdlib-docs-phase1.md) — **Stdlib-docs Phase 1 / AC1 DONE 2026-06-23** (trunk `main`): activated v-stdlib's dormant doc-generation half (the drift-gate half was already ported). Adapted `gen-manifest.py` (reconciled errors file `vsl-errors.json`→`dist/errors.json` to match m-stdlib + DoD; now differs from m-stdlib in only glob/manifest-name/docstring — one maintained generator, R-DRIFT). Ported `gen-skill.py` + v-flavored `skill-patterns.md`; **`write-module-frontmatter.py` had to DIVERGE** (m-stdlib backfills existing pages; v-stdlib had zero → it CREATES stubs + generates `docs/modules/index.md`). Golden test fixture in **`tools/fixtures/VSLGOLD.m`** (out of src/tests globs so fmt can't shift line numbers) → `tools/test-manifest-golden.py` (`make manifest-golden`), TDD red→green. Outputs: `dist/vsl-manifest.json` (17 mods/117 labels) + `dist/errors.json` (7 U-VSL*) + `dist/skill/` + 17 `docs/modules/vsl*.md` stubs + index. Makefile `manifest{,-check}`/`frontmatter`/`skill{,-check,-install}`/`manifest-golden`; `manifest-check skill-check manifest-golden` in `make gates` + `ci.yml`. `skill-install`→`~/claude/skills/v-stdlib/`. **NEXT: Phase 2** docs-check completeness gate (ships green here). Grammar copied to `docs/guides/m-doc-grammar.md` (interim; org-level in P5).
- [phase5-ga-forward-port](phase5-ga-forward-port.md) — **Traffic-tap Phase 5 / M4 (GA) deliverables FORWARD-PORTED onto Phase-6 main, committed to `main` 2026-06-23.** GA work (KIDS install/back-out + production fidelity-run task) was live-proven on branch `phase5-ga-kids-backout` / **PR #15**, but that branch predates Phase-6 (FU-4/5) → its diff reverts `VSLRPCWRAP`/schema-v1 ⇒ **PR #15 STALE, do not merge — superseded by this port.** Net-new: **`VSLTAPBO`** (back-out/verify-clean, risk G-UNINST — reverses the XPAR-params/TaskMan/`^XTMP("VSLTAP")`/`^VSLTAP` footprint `v-pkg uninstall` orphans), **`VSLTAPRUN`** (periodic fidelity task `reconcilePersist`/`cadence`/`schedule`/`run`/`fidelityNow` — closes the console `pending` loop), **`VSLTAP.seed`** (XPAR→cfg), **`VSLS3.list`**, **10 tap XPAR `#8989.51` params**, **patch `*2`→`*3`** (kept `VSLRPCWRAP`), the **DIBRG**. Only adaptation: VSLTAPRUNTST `env()` → 2-arg `$$envelope^VSLS3(.rec,.opt)`. **Bare dual-engine 15 suites/309/0 both engines (VSLTAPBOTST 12/12 + VSLTAPRUNTST 8/8); engine-free gates green (icr 26, check-kids golden @ patch 3).** OWED next: live install→back-out re-proof on vehu+foia (now w/ VSLRPCWRAP co-resident) + MinIO fidelityNow matrix + real-S3 flip. Shared: [[rpc-traffic-s3-streaming-proposal]]. Builds on [[fu5b1-rpcwrap-glue]] / [[phase4-fidelity-persist]].
- [fu5b1-rpcwrap-glue](fu5b1-rpcwrap-glue.md) — **Traffic-tap FU-5 increment 5B.1 DONE 2026-06-23** (the bare-engine half of FU-5 5B; the live patched-XWBBRK install + non-interference proof + FU-21 are 5B.2/next). New **`VSLRPCWRAP`** — the XWB broker-dispatch wrap glue the `CALLP^XWBBRK` splice calls: `req()`/`resp()` each OWN the FU-4 fence (reuse `$$nakedRef^VSLRPCTAP`), gate on `$$captureOn`, build a schema-v1 rec from broker process vars (XWB CAPI node / `XWB(3,"P",*)` `$C(1)`-joined / XWBSEC→denied / XWBP+XWBPTYPE→result by 1=scalar·2-3=array·4=global / DUZ·$J·XWBTIP:XWBTSKT), tee via `$$teeRec^VSLTAP`; `$J`-LOCAL counter VSLWCC/VSLWCID correlates req↔resp. **Splice re-pinned live byte-identical YDB↔IRIS**: req after `CALLP^XWBBRK:153`, resp inside the `:155` success block after `:158` (before `:160 K XWB`). KEY: the splice line is **UNCONDITIONAL** (`D req^VSLRPCWRAP`) — a `D:$G(^flag)` guard would corrupt the broker naked indicator BEFORE the fence; `XUS SET SHARED` early-Qs at `:148` (coverage gap); XWBTIP/XWBTSKT/DUZ are EN^XWBTCPC params, XWBPTYPE set at PRSA:76. **GOTCHA: M escapes `"` by doubling (`""`) not `\"`; a `\"` in an assertion desc → compile error → `m test` shows 0/0 (suite abort, not a fail) and lint misses it.** New `VSLRPCWRAPTST` (broker simulated): 33/33 dual-engine; full bare green both; IRIS cov 98.2%; gates+KIDS(15 rtns)+namespaces green. Splice doc: docs `docs/discoveries/fu-5b-callp-splice.md`. Shared: [[rpc-traffic-s3-streaming-proposal]]. Stacks on [[fu5a-schema-v1-capture]].
- [fu5a-schema-v1-capture](fu5a-schema-v1-capture.md) — **Traffic-tap FU-5 increment 5A DONE 2026-06-23** (branch `s3tap-fu5a-schema-v1`, stacked on FU-8/FU-9): the bare-engine capture path now emits **schema v1** (FU-14/15/17/18 built; the live `CALLP^XWBBRK` wrap + non-interference proof + FU-21 are **5B/next**). **`VSLTAP` cache layout v2** = an 18-piece `^`-header + `"p",i` RAW chunks + `"hc",i` + a `"g"` MERGE subtree (`$$appendRec`/`$$teeRec`/`$$hdr`/`$$isV2`/`$$chunk`; v1 `$$append` kept; recursive KILL reaps the subtree). **`capture^VSLRPCTAP(.rec)`** takes a rich descriptor behind the unchanged FU-4 fence; `$$callId`=`station-$J-ctr` (a $J-**local** counter, no hot global). **`$$envelope^VSLS3(.rec,.opt)`** = the schema-v1 wire members, ONE shape (`direction` discriminates: req→`denied`, resp→`result_kind`), wire_len/payload_sha256 over RAW; **dual-mode `$$drain`** frames v2-header OR v1-legacy and serializes a GLOBAL-ARRAY `"g"` snapshot off-path via `$$gSerialize` (FU-17 = one in-path `MERGE @gref`; broker spares `^XTMP(`). **`$$drops^VSLTAPFC`** = `rpc_error`/`rpc_denied` by `call_id` reconcile (FU-15) → `_fidelity` counts. KEY: ring stores RAW, encode/serialize/hash deferred to drain (in-path minimal); `$name` root ends `)` → compare descendants vs root-sans-paren in `gSerialize`; M-MOD-036/024 disables (internal `@^XTMP` indirection + by-ref `hdr`). **Dual-engine 190/190; full bare green both; IRIS cov 96.6% on the 4 changed modules; gates+KIDS green.** Shared: [[rpc-traffic-s3-streaming-proposal]]. Extends [[fu8-fu9-ring]] / [[fu4-naked-ref-fence]].
- [fu8-fu9-ring](fu8-fu9-ring.md) — **Traffic-tap FU-8 (atomic `$INCREMENT` seq) + FU-9 (always-on ring / egress-gate split) DONE 2026-06-23** (branch `s3tap-fu8-fu9-ring`, stacked on FU-4): `write1` seq via atomic `$increment` (was racy read-then-write → lost+dup records). Gate SPLIT: `$$captureOn` (armed∧¬disabled, always-on, `append`) vs `$$enabled` (captureOn∧consumer, egress/drain/state/HL7). **HL7 tail STAYS consumer-gated** (#772 is persisted/replayable — only the ephemeral RPC ring goes always-on). `alwayson` subsumed → legacy no-op (v-web reads cfg). FU-8's head-ahead-of-data window forced a **gap-safe drain** (`$$present(seq)`; ship contiguous committed prefix, `drainTo(last)` not head). Behavior inversion (no consumer→ring still captures) → rewrote several tests; bench idle arm → kill-switch path. GOTCHA: `JOB` unavailable in bare engines (no true OS-parallel test — invariant+primitive, honest). Dual-engine **198/198**; IRIS cov 95%. Follow-up: v-web VWEBT display labels. **NEXT: FU-5 wrap.** Shared: [[rpc-traffic-s3-streaming-proposal]]. Extends [[fu4-naked-ref-fence]].
- [fu4-naked-ref-fence](fu4-naked-ref-fence.md) — **Traffic-tap FU-4 (R-NAKED fence) DONE 2026-06-23** (branch `s3tap-fu4-naked-ref-fence`): `capture^VSLRPCTAP` saves/restores the caller's naked indicator on every exit (finally path; `set zz=$d(@nref)` last). **GOTCHA: the naked-ref SVN differs by engine — YDB `$REFERENCE`, IRIS `$ZREFERENCE`** (`$zreference`→INVSVN on YDB; plan's "both" was wrong) → read it via **XECUTE** of an engine-selected assignment (no literal SVN compiled). New `VSLTAPFENCETST` (AC-1…AC-7) + post-write `faultinjectpost` seam. **Dual-engine 12/12; bare 186/186; IRIS cov 98%** (YDB collector env-broken). AC-7 extended-ref proven IRIS-only (YDB region-ref = documented placeholder). Gates FU-5. Shared: [[rpc-traffic-s3-streaming-proposal]]. Extends [[phase2-vsltap]].
- [kids-ship-all-routines](kids-ship-all-routines.md) — `kids/vsl.build.json` (`VSL*1.0*2`) was a stale 8-routine subset; now ships **all 13 `VSL*` routines** (adds VSLRPCTAP/VSLS3/VSLTAP/VSLTAPFC/VSLTAPHL) so VSL installs in its entirety. Needs **`"allowLongNames": true`** (`VSLRPCTAP`=9 chars) — org routine-name policy ADR. `VSL.kids` rebuilt 48→100 KB; all 7 `make gates` green. Branch `ship-all-routines`. Shared: [[routine-name-length-policy]] (docs repo).
- [phase4-fidelity-persist](phase4-fidelity-persist.md) — **Traffic-tap Phase 4 / M3** (branch `phase4-fidelity-persist` off `ship-all-routines`): `VSLTAPFC` gains **`do persist(res,ts)`** (stores the `_fidelity` manifest at `^VSLTAP("fc","last")`) + **`$$lastFidelity()`** (passive getter, `""`→console "pending") so the v-web SSE console can read the last fidelity result without re-running a comparison. **VSLTAPFCTST 28/28 dual-engine**; `make check-fast` green; `VSL.kids` regenerated (14 rtns, no list change). Leaf for the v-web console; v-web consumes via `--routines src`. Extends [[phase3-egress-fidelity]].
- [phase3-egress-fidelity](phase3-egress-fidelity.md) — **Traffic-tap Phase 3 / M2** (branch `phase3-egress-fidelity`/`ship-all-routines`, unmerged): **`VSLS3`** (LDJSON envelope + §11 keys + config seam + consumer-gated `$$drain`) + **`VSLTAPFC`** (byte-equality verify/matches/reconcile/manifest) + **`VSLTAP $$drainTo`** + **`VSLHL7TAP`** (HL7 store-tail). **All dual-engine green; live MinIO round-trip `VSLS3E2ETST` GREEN on BOTH engines** (egress blockers G-HTTP-YDB/IRIS-GET resolved via m-test-engine `0.2.0`). **`VSLHL7TAP` DONE 2026-06-20 (17/17; tap regression 144/144; shipped in KIDS→14 rtns): G-HL7HOOK resolved by a LIVE `vehu` probe + DD** — `do tail()` is a consumer-gated/fenced store-tailer over BOTH legacy `#772` (`^HL(772,IEN,"IN",seq,0)`, package-managed not a DD field) AND HLO `#778/#777` (`^HLB`/`^HLA`) from a persisted `$ORDER` cursor (file 0-node piece-3 is STALE), teed via `$$tee^VSLTAP`. HLO leg DD-grounded (vehu had no HLO data). `make` byte-mode default. **GOTCHA (4th sighting):** `DO` of an extrinsic (`tee^VSLTAP`) → YDB 0/0 abort → use `$$`. check-kids now GREEN. **Option-A round-trip matrix WIRED INTO `make ci` 2026-06-20 (3.4):** self-contained `test-s3-matrix` (vendored `scripts/s3-testbed.sh` MinIO up → `VSLS3E2ETST` on both engines → down); `make ci` = engine-free gates + `test-bare` (10 bare-green suites both engines) + matrix, **green exit 0**. **GitHub `ci.yml` engine-CI WIRED 2026-06-20:** self-contained `engine-ydb` (HARD: build m + clone m-stdlib@master + m-test-engine:0.2.0 + vendored MinIO + test-bare + test-s3) + `engine-iris` (fail-soft, PR-only, iris-community); **CONFIRMED GREEN in Actions on draft PR #13** (2026-06-21, run 27890221864): `engine-ydb` bare 165/0 + round-trip `VSLS3E2ETST` 6/6; `engine-iris` (fail-soft) also bare 165/0 + round-trip 6/6 — iris-community handled the `%Net` egress to MinIO with `--namespace USER`, no tuning needed. **REMAINS:** HLO live-data validation; VSLTAPFC HL7 live-periodic hook. (PR #13 left as DRAFT — merge is a user action.) **Option B (socket, 3.5) DEFERRED 2026-06-20** until a zero-DB-write site needs it (A covers it at near-zero footprint; B reintroduces the AWS-mirroring per-host-sidecar dependency). Extends [[phase2-vsltap]].
- [phase2-vsltap](phase2-vsltap.md) — **Traffic-tap Phase 2 / M1 DONE** (2026-06-19, branch `phase2-vsltap`, unmerged): the non-interference capture core — **`VSLTAP`** (rolling `^XTMP` ring, three-way capture gate, self-fenced bounded append, auto-failover + `_offwindows`, state machine) + **`VSLRPCTAP`** (the fenced fire-and-forget tee at the injected VSLRPC chokepoint) + **`VSLTAPHL`** (counters/percentiles, A/B latency watchdog, standby readiness probe + synthetic canary). **Dual-engine GREEN: YDB 73/73 + IRIS 73/73 on BARE engines** (no VistA/S3/network — egress-independent). 3-arm non-interference benchmark is a **`make ci`** gate (D-7 bounds 250µs/2500µs). **GOTCHAS:** (1) an arg-less `QUIT` in a flag-`$ETRAP` trap fired in an EXTRINSIC (`$$`) frame raises **M17 NOTEXTRINSIC** and aborts 0/0 → the risky write must run in a **DO**-invoked frame (extends the M4 zgoto gotcha [[m4-vslsec-vsllog]]); (2) **`new $test` ABORTS on IRIS** → save/restore `$TEST` by hand (`set t=$test … if t`). `^XTMP` added to the namespace-gate scratch exemption. `make check-kids` is **pre-existing red on main** (v-pkg drift), not a regression. Next: Phase 3 (M2) egress + fidelity.
- [m6.5-vslsec-secid-binding](m6.5-vslsec-secid-binding.md) — VSL/MSL **M6.5 (v-stdlib slice)**: `VSLSEC` gains **`$$bySecid`** (SecID → NEW PERSON #200 IEN via **`EN1^XUPSQRY`**, **ICR 4575 Controlled Subscription**) + the pure `$$parseQry` array parser — the VistA half of the *validate-token-not-PIV* auth binding (a validated token's SecID claim → local #200/DUZ, mirroring Kernel's XUSAML/XUESSO2; token validation itself is engine-neutral in m-stdlib [[m6.5-stdjwt]] / extends [[m4-vslsec-vsllog]]). **EN1^XUPSQRY corpus-grounded**: SecID in param 2, null last-name param 3, **by-ref array** result (`result(1)`=found-flag 1/0, `result(1,0)` ^-piece 2 = IEN), NOT a string; no Supported non-CS SecID→IEN API; SecID=#200 #205.1. Made VSLSECTST **dual-engine** (setup's `$$DT^XLFDT` no longer aborts bare). **Bare YDB+IRIS 11/11; vehu 12/12; foia-t12 12/12**; check-icr clean (4575 registered). Branch `m6.5-secid-binding`. Next: v-web VWEBA. Owed: shared docs-repo tracker M6.5 row.
- [m5-vsltask-vslbld](m5-vsltask-vslbld.md) — VSL/MSL **M5 DONE** (2026-06-17): **VSLTASK** (TaskMan persistent-listener / process seam) + **VSLENV** (self-contained KIDS env-check / XPDENV hook) + **VSLBLD** (full KIDS base build + env-check binding / packaging seam). **Dual-engine GREEN 23/23** (vehu YDB + foia-t12 IRIS); suite 56/56 no regression. **GROUNDED Q1: TaskMan is LIVE on BOTH engines** (`$$TM^%ZTLOAD()`=1, heartbeat fresh) → liveness + cooperative-stop + API + loud error contract are **live-green** (a NARROWER skip than M2); only the **destructive self-restart observation is SOFT-SKIPPED** (a PSET-persistent task is un-KILLable + needs a resident body → runaway-unsafe; restart contract bound+documented). VSLTASK binds `^%ZTLOAD` (**ICR #10063**, whole API): `$$running`/`$$stop`/`$$persist`/`$$schedule`; loud `,U-VSL-TASK-...,`. VSLENV reports engine/version/Kernel(`$$VERSION^XPDUTL` **#10141**)/TLS(`$$GET^XPAR` **#2263**). VSLBLD `$$manifest`/`$$envCheck`/`$$requireBase` (`$$PATCH^XPDUTL` #10141 — the **R6** version-skew check); loud `,U-VSL-BLD-...,`. **Q2: KIDS base matured IN PLACE** → `VSL*1.0*2`, **all 8 routines** + envCheck VSLENV + Required Build MSL\*0.1\*1; **install→verify(8 routines+param)→back-out→verify-clean GREEN both engines** (status 3 / exit 3). **Lane A NO-OP** (pin stays **v0.9.0**). Gate change: `gen-icr.py` now enforces the **XPD** namespace as L4. Gates: check-icr 17 / check-citations 17 (vs gold) / check-namespaces 8 / check-kids golden. Branch `m5-vsltask-vslbld`. Next: M6 (VWEB).
- [m4-vslsec-vsllog](m4-vslsec-vsllog.md) — VSL/MSL **M4 DONE** (2026-06-16): **VSLSEC** (security/authz seam S5) + **VSLLOG** (audit sink S3). **Dual-engine GREEN 11/11** (vehu YDB + foia-t12 IRIS); suite 33/33 no regression. **VSLSEC = authz-only** — GROUNDED that there is **no portable Kernel generic-hash** (`$$SHAHASH^XUSHSH` absent on vehu; classic `^XUSHSH` returns a constant on both) so crypto stays in **STDCRYPTO**; binds `$$hasKey` over **`^XUSEC`** (Supported reference, notional ICR; a DENY is a normal `0`), `$$duz` (#200 IEN), `$$user` (#200 NAME via **VSLFS reuse**); loud **`,U-VSL-SEC-ARG,`** on a malformed call. **VSLLOG = first v→v composition** — reuses VSLFS (no DBS re-bind), audit line via `$$now^STDDATE` (v→m), maps VSLFS DIERR → **`,U-VSL-LOG-WRITE,`**; no `@icr` (no direct L4 call). **Lane A NO-OP** (no MSL seam change; pin stays **v0.9.0**). GOTCHA: `zgoto`-`$ETRAP` aborts the resident harness 0/0 → use the flag-based `$ETRAP` (clear OUR trap before re-raising). Gates: check-icr 9 / check-citations 9 / check-namespaces 5. MailMan alert + `$$inContext` (CRCONTXT) deferred. Branch `m4-vslsec-vsllog`. Next: M5 (VSLBLD/VSLTASK).
- [m3-vslfs](m3-vslfs.md) — VSL/MSL **M3 Lane B DONE** (2026-06-16): **VSLFS** binds the `STDKV` storage seam (MSL **v0.9.0**) to VistA's FileMan DBS — `$$set`=`UPDATE^DIE` (returns resolved IENS; `"+1,"` adds), `$$get`/`$$exists`=`$$GET1^DIQ`, `$$kill`=`FILE^DIE` with FDA **`.01="@"`** (**no `DELETE^DIE` exists**; `^DIK`/direct KILL forbidden). Re-pinned `msl_ref` v0.8.0→v0.9.0. **Dual-engine GREEN 7/7** (vehu + foia-t12) over **#8989.51** (free-text `.01`, no other required fields → safe ZZ throwaway record; no DD install). DIERR→**`,U-VSL-FS-DIERR,`** `$ECODE` + `$$lastError`; MSG_ROOT="ERR" keeps errors private. **No `$ZVERSION` arm** (FileMan DBS portable). ICR **notional** (`@icr DBS` marker — gen-icr.py taught NOTIONAL_MARKERS; see [[notional-dbia-not-a-blocker]]). 3 boundaries green; suite **22/22** no regression. Branch `m3-vslfs` **stacked on `m2-vslio`** (unmerged). Next: M4 (VSLSEC+VSLLOG).
- [t1.3-vsl-kids](t1.3-vsl-kids.md) — VSL T1.3 (2026-06-16): **the VSL layer packaged as a KIDS build**. `kids/vsl.build.json` = VSLCFG routine + **`VPNG GREETING` #8989.51 PARAMETER DEFINITION** (SYS, free text) + **Required Build on `MSL*0.1*1`** (action LEAVE GLOBAL; pin stays MSL v0.7.0). **install→verify→uninstall→verify-clean GREEN on BOTH engines** over the driver (`v-pkg`; vehu YDB + foia-t12 IRIS). Normalized export committed at **`dist/kids/VSL.kids`** + **`make check-kids`** deterministic/golden drift gate (in `gates`; SKIP-green w/o v-pkg). Param named for the **consumer** (VPNG), matching VSLCFG.m's doc default `"hello"`. **Required-build posture (a)**: emitted into #9.6/MBREQ (faithful) but NOT enforced (direct-populate install bypasses the interactive KIDS prereq check). Sits on the v-pkg KRN capability (v-pkg main `2a3f273`). Next: T1.4 VPNG consumer.
- [t1.2-vslcfg](t1.2-vslcfg.md) — VSL T1.2 (2026-06-16): **VSLCFG**, the first VSL* module — binds the STDENV config seam to XPAR at the **SYS entity** (`$$get`=`$$GET^XPAR("SYS",…)`, `$$set`=`EN^XPAR("SYS",…)`), validated live on vehu. **All 3 determinism boundaries GREEN** (① re-pin `msl_ref`→v0.7.0 carrying real STDENV; ② check-icr ICR #2263; ③ check-citations vs gold corpus). Citation reconciled: **XU/krn_8_0_dg_toolkit_ug / ICR #2263**, not the plan's XT guess. **Keystone unblock:** driver→live XPAR via `M_YDB_GBLDIR`/`M_YDB_ROUTINES`. **Remaining blocker:** `m test --docker` honors gbldir but NOT M_YDB_ROUTINES → VSLCFGTST aborts 0/0 (XPAR routines unresolved); fix = layer the resident routine base in the m test/m-ydb path (m-cli/m-ydb session) or test-in-place `--resident`.
- [meta-root + owed VSLSEED filer](meta-root-and-owed.md) — layer declared in **root `repo.meta.json`** (migrated off `dist/` 2026-06-15, Phase B item 1); the owed `fileViaDie^VSLSEED` FileMan filer (re-homed from m-stdlib STDSEED per the G2 waterline decision) lands here when a v-layer seeding consumer needs it.
- [t0b4-msl-seam-pin](t0b4-msl-seam-pin.md) — VSL T0b.4 (v-stdlib leg, 2026-06-15): the **cross-repo MSL seam-contract pin** — `dist/msl-seam-pin.json` (pins MSL `v0.6.0` + frozen `seams` copy) + `tools/msl_seam_pin.py` drift gate (`make check-msl-pin`). Reads the sibling MSL @ tag via `git show`; **SKIP-green when unreachable** (so it SKIPs in CI today, asserts at dev-time); fetch-at-tag path deferred to T1.1. Don't conflate with v-stdlib's own (VSL*) `dist/seam-snapshot.json`.
- [t0b3-drift-gates](t0b3-drift-gates.md) — VSL T0b.3 (v-stdlib leg, 2026-06-15): the **four drift gates** mirrored from m-stdlib (tools/ copied verbatim except gen-manifest's `VSL*` glob). All **green-empty** (no VSL* modules yet); red-on-violation proven. `repo.meta.json` gained `namespaces: {VSL}`; `ci.yml` runs them via an `m-ci.yml` caller with `engine-targets: ""` (engine-free). fmt/lint left out of CI (Makefile `M` default is a local path). Next: T0b.4 freeze+pin the seam contract.
- [m2-vslio](m2-vslio.md) — VSL/MSL **M2 Lane B DONE** (2026-06-16): `VSLIO` binds the `STDNET` socket seam (MSL **v0.8.0**) to VistA's Kernel device handler — **outbound TCP via `CALL^%ZISTCP` (ICR #2118)**; the CLIENT subset only (VistA has no Supported listen/accept; server side stays in STDNET). Re-pinned `msl_ref` v0.7.0→v0.8.0 (first real fetch-at-tag). **vehu(YDB) 10/10** (tier-1 POP=0 + tier-2 echo) · **foia-t12(IRIS) 6/6** (connect-failure proves the binding; loopback soft-skips on STDNET's deferred IRIS leg). TLS-gap loud (`$$tlsAvailable`=0, `$$connectTls` raises `U-VSLIO-NOTLS`). **KEY:** the gold doc's input-var convention for `CALL^%ZISTCP` is WRONG — it's argument-passed `CALL(IP,SOCK,TO)`; leaves the socket device CURRENT (restore `$IO`). 3 boundaries green (check-msl-pin@v0.8.0 / check-icr 4 / check-citations 4).
